<div class="flex flex-wrap md:flex-nowrap justify-center lg:justify-between items-center shadow p-4 h-full md:h-44">
  <div class="mb-6 md:mb-0 px-4">
    <h5 class="text-lg font-semibold inline-flex gap-2">
      <span>Profile Completion Status: <%= progress_bar %></span>
      <span class="relative flex h-3 w-3">
        <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-[#9c9e83] opacity-75"></span>
        <span class="relative inline-flex rounded-full h-3 w-3 bg-[#B7BAA7]"></span>
      </span>
    </h5>
    <p class="text-gray-500 text-sm mb-2">Complete at least 90% of your profile to appear in search engine results. 
      <br> <a href="<%= public_view_users_profiles_path %>" class="underline mt-4 inline-block">View profile as Public</a> </p>
  </div>
  <div>
    <script src="https://cdn.jsdelivr.net/gh/alpinejs/alpine@v2.7.3/dist/alpine.min.js" defer></script>
    <div class="overflow-hidden relative" x-data="{ circumference: 50 * 2 * Math.PI, percent: <%= progress_bar_count %> }">
      <svg class="w-32 h-32" x-cloak aria-hidden="true">
        <circle class="text-gray-100  "
          stroke-width="10"
          stroke="currentColor" fill="transparent"
          r="50"
          cx="60"
          cy="60"
          />
        <circle class="text-gray-300"
          stroke-width="10"
          :stroke-dasharray="circumference"
          :stroke-dashoffset="circumference - percent / 100 * circumference"
          stroke-linecap="round"
          stroke="currentColor"
          fill="transparent"
          r="50"
          cx="60"
          cy="60"
        />
      </svg>
      <span class="absolute top-1/2 -translate-y-1/2 w-full text-center text-xl text-gray-800" x-text="`${percent}%`"></span>
    </div>
  </div>
</div>